# Sample Data

This directory stores non-production sample datasets used for local experimentation and offline demonstrations.

- `matches3.csv`
- `premier_league_latest_results.csv`

For deterministic multi-sport demo events used by the web app fallback, see:
- `webapp/data_sources.py` (`generate_demo_events`)

